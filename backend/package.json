{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend API for Indian ERP + AI Decision Intelligence demo",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:studio": "prisma studio"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
